{{- $ingressClassName := .Values.global.lumenvox.ingress.className -}}
{{- if ne $ingressClassName "istio" }}
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: reporting-api-ingress-grpc
  namespace: {{ default .Release.Namespace .Values.global.defaultNamespace }}
  annotations:
    # Internal staff route: internalAllowlist restricts it by source IP.
    {{- include "lumenvox-common.nginxAnnotations" (dict "kind" "grpc" "root" $ "allowlist" .Values.global.lumenvox.ingress.internalAllowlist) }}
spec:
  {{- if $ingressClassName }}
  ingressClassName: {{ $ingressClassName }}
  {{- end }}
  {{- if not .Values.global.lumenvox.ingress.disableTls }}
  tls:
  - hosts:
    - "reporting-api{{ .Values.global.hostnameSuffix }}"
    secretName: {{ .Values.global.lumenvox.gateway.tlsSecretName }}
  {{- end }}
  rules:
  - host: "reporting-api{{ .Values.global.hostnameSuffix }}"
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: reporting-api-service
            port:
              name: grpc
{{- else }}
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: reporting-api-grpcroute
  namespace: {{ default .Release.Namespace .Values.global.defaultNamespace }}
spec:
  parentRefs:
    - name: {{ .Values.global.lumenvox.gateway.name }}
      namespace: {{ include "lumenvox-common.gatewayNamespace" . }}
      sectionName: web
  hostnames:
    - "reporting-api{{ .Values.global.hostnameSuffix }}"
  rules:
    - timeouts:
        request: "0s"
      backendRefs:
        - name: reporting-api-service
          port: 5000
{{- end }}
