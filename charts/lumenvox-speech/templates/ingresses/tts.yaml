{{- if .Values.global.lumenvox.metrics.enableEndpoints }}
{{- $ingressClassName := $.Values.global.lumenvox.ingress.className -}}
{{- range .Values.global.ttsLanguages }}
---
{{- if ne $ingressClassName "istio" }}
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: tts-{{ .name | replace "_" "-" }}-ingress
  namespace: {{ default $.Release.Namespace $.Values.global.defaultNamespace }}
spec:
  {{- if $ingressClassName }}
  ingressClassName: {{ $ingressClassName }}
  {{- end }}
  rules:
  - host: "tts-{{ .name | replace "_" "-" }}{{ $.Values.global.hostnameSuffix }}"
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: tts-{{ .name | replace "_" "-" }}-service
            port:
              name: http
{{- else }}
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: tts-{{ .name | replace "_" "-" }}-httproute
  namespace: {{ default $.Release.Namespace $.Values.global.defaultNamespace }}
spec:
  parentRefs:
    - name: {{ $.Values.global.lumenvox.gateway.name }}
      namespace: {{ include "lumenvox-common.gatewayNamespace" $ }}
      sectionName: web
  hostnames:
    - "tts-{{ .name | replace "_" "-" }}{{ $.Values.global.hostnameSuffix }}"
  rules:
    - backendRefs:
        - name: tts-{{ .name | replace "_" "-" }}-service
          port: 5110
{{- end }}
{{- end }}
{{- end }}
