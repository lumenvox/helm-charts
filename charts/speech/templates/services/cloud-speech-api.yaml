apiVersion: v1
kind: Service
metadata:
  name: speech-api-service
  namespace: {{ default .Release.Namespace .Values.defaultNamespace }}
spec:
  selector:
    app: speech-api
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 80
