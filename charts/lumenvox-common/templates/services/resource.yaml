apiVersion: v1
kind: Service
metadata:
  name: resource-service
  namespace: {{ default .Release.Namespace .Values.global.defaultNamespace }}
spec:
  selector:
    app: resource
  ports:
    - name: grpc
      port: 8080
      targetPort: grpc
    - name: http
      port: 5110
      targetPort: http
