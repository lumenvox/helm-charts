apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: engineresource-ingress
  namespace: {{ .Release.Namespace }}
  annotations:
    traefik.ingress.kubernetes.io/router.entrypoints: web
spec:
  rules:
    - host: {{ .Values.hostnamePrefix }}-engineresource.com
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: engineresource-service
                port:
                  number: 90
---
apiVersion: traefik.containo.us/v1alpha1
kind: IngressRoute
metadata:
  name: engineresource-ingress-grpc
  namespace: {{ .Release.Namespace }}
spec:
  entryPoints:
    - grpc
  routes:
    - kind: Rule
      match: Host(`{{ .Values.hostnamePrefix }}-engineresource-grpc.com`) && Headers(`Content-Type`, `application/grpc`)
      priority: 11
      services:
        - name: engineresource-service
          port: 80
          scheme: h2c
